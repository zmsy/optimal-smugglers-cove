{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "optimal-smugglers-cove config",
  "type": "object",
  "properties": {
    "max_bottles": {
      "type": "integer",
      "description": "Maximum total number of selected items (bottles + non-bottles) (CP-SAT limit).",
      "default": 10,
      "minimum": 1
    },
    "alpha": {
      "type": "number",
      "description": "Tradeoff weight between coverage and objective score",
      "default": 0.3
    },
    "use_log_scores": {
      "type": "boolean",
      "description": "If true, use log-transformed scores when building the objective.",
      "default": true
    },
    "score_scale": {
      "type": "number",
      "description": "Scaling applied to computed scores before converting to integer coefficients.",
      "default": 1.0
    },
    "use_rank_scores": {
      "type": "boolean",
      "description": "If true, use rank-based scores instead of raw/log scores.",
      "default": false
    },
    "ingredient_mode": {
      "type": "string",
      "description": "Controls which ingredient types are selectable.\n- bottles_only: only bottle-true ingredients\n- non_bottles_only: only bottle-false ingredients\n- both: bottle and non-bottle ingredients",
      "default": "bottles_only",
      "enum": ["bottles_only", "non_bottles_only", "both"]
    },
    "objective_scale": {
      "type": "integer",
      "description": "Multiplier to keep CP-SAT objective coefficients as integers (larger => larger scaling).",
      "default": 1000000,
      "minimum": 1
    }
  },
  "additionalProperties": false,
  "required": [
    "max_bottles",
    "ingredient_mode"
  ]
}
