# optimal-smugglers-cove

Using a CSP-Solver to find the best bang for your buck with Martin Cate's "Smuggler's Cove"

## Running the code

```sh
uv run python src/csp.py --output-dir ./solver_outputs --config ./config.json
```

## Config

Done in `config.json`, inline schema provided.
